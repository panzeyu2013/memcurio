- insert:
    - id: memcurio
      name: '@memcurio/dsh-plugin'
      inject: [tools, llm, sessions, settings]
      config:
        scope: workspace
        injectContext: true
        registerTools: true
